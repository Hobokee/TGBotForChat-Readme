# Telegram Bot на aiogram 3

Бот для управления чатами с функциями модерации, предупреждений и управления игроками.

## Возможности

- 🔇 **Мут пользователей** с указанием времени (например: `1d 2h 3m 4s`)
- 🔊 **Размут пользователей**
- 🔨 **Бан пользователей** с возможностью указать время или сделать перманентным
- ✅ **Разбан пользователей**
- ⚠️ **Система предупреждений** с автоматическим баном при достижении лимита и автоматическим удалением через указанное время (по умолчанию 7 дней)
- 🎮 **Управление игроками** - добавление пользователей в список игроков
- 👮 **Система админов** - управление правами на команды модерации через `/admin-add`
- 🛠️ **Debug команды** для разработчика
- 📊 **Автоматическое добавление пользователей в БД** при обнаружении в чате
- ⏰ **Автоматическое снятие мутов/банов/варнов** по истечению времени
- 🔒 **Отключение команд** - возможность отключать команды модерации

## Система прав доступа

**Важно:** Команды модерации могут использовать только:
- Разработчик (указан в `ADMIN_ID` в `.env`)
- Пользователи, добавленные через команду `/admin-add` в конкретной группе

**Без команды `/admin-add` пользователь НЕ может использовать команды модерации, даже если он является администратором в Telegram!**

## Команды

### Управление админами (только разработчик)

- `/admin-add @пользователь` - Добавить админа в группу
  - Дает право на использование всех команд модерации в этой группе
  - Работает только в группах
  - Можно ответить на сообщение: `/admin-add`
  
- `/admin-remove @пользователь` - Удалить админа из группы
  - Убирает право на использование команд модерации
  - Работает только в группах
  - Можно ответить на сообщение: `/admin-remove`

### Команды модерации

**Требуют права админа (добавлены через `/admin-add` или разработчик)**

- `/mute @пользователь [время] [причина]` - Заглушить пользователя
  - Пример: `/mute @user 1d 2h 3m 4s Нарушение правил`
  - Если время не указано - по умолчанию 1 час
  - Можно ответить на сообщение: `/mute 1h`
  
- `/unmute @пользователь` - Размутить пользователя
  - Можно ответить на сообщение: `/unmute`
  
- `/ban @пользователь [время] [причина]` - Забанить пользователя
  - Пример: `/ban @user 7d Спам`
  - Если время не указано - перманентный бан
  - Можно ответить на сообщение: `/ban 1d`
  
- `/unban @пользователь` - Разбанить пользователя
  - Можно ответить на сообщение: `/unban`
  
- `/warn @пользователь [время] [причина]` - Выдать предупреждение
  - Пример: `/warn @user 3d Нарушение правил`
  - Если время не указано, варн автоматически удалится через 7 дней (настраивается в `config.json`)
  - При достижении лимита предупреждений пользователь автоматически банится
  - Бот отправляет уведомление в чат и в личные сообщения пользователю
  - Можно ответить на сообщение: `/warn 1d Нарушение`
  
- `/add_player @пользователь` - Добавить пользователя в список игроков
  - Можно ответить на сообщение: `/add_player`

### Debug команды (только разработчик)

- `/debug_stats` - Показать статистику базы данных
  - Показывает количество пользователей, игроков, активных варнов, мутов и банов
  
- `/debug_user <user_id>` - Показать информацию о пользователе
  - Пример: `/debug_user 123456789`
  - Показывает полную информацию о пользователе из БД
  
- `/debug_chat` - Показать статистику текущего чата
  - Работает только в группах
  - Показывает статистику по варнам, мутам и банам в текущем чате
  
- `/debug_help` - Показать справку по debug командам
  - Выводит список всех доступных debug команд
  
- `/debug-off` - Отключить все команды модерации глобально
  - Отключает команды: `mute`, `unmute`, `ban`, `unban`, `warn`, `add_player`
  - Работает глобально для всех чатов
  
- `/debug-off-command <команда>` - Отключить конкретную команду
  - Пример: `/debug-off-command mute`
  - Имя команды указывается без слеша
  - Если команда в группе - отключается для этого чата
  - Если команда в ЛС - отключается глобально
  
- `/debug-on-command <команда>` - Включить отключенную команду
  - Пример: `/debug-on-command mute`
  - Имя команды указывается без слеша
  - Если команда в группе - включается для этого чата
  - Если команда в ЛС - включается глобально

## Формат времени

Время можно указывать в следующем формате:
- `d` - дни (например: `1d`)
- `h` - часы (например: `2h`)
- `m` - минуты (например: `3m`)
- `s` - секунды (например: `4s`)

Можно комбинировать: `1d 2h 3m 4s`

Примеры:
- `1d` - 1 день
- `2h 30m` - 2 часа 30 минут
- `1d2h3m4s` - можно писать слитно или раздельно

## Важные примечания

1. **Права доступа**: Для использования команд модерации пользователь должен быть добавлен через `/admin-add`, даже если он администратор в Telegram.

2. **Отключение команд**: Команды модерации можно отключать через debug команды. Глобальное отключение действует для всех чатов.

3. **Автоматическое удаление**: Варны автоматически удаляются по истечению времени (по умолчанию 7 дней, если не указано другое).

4. **База данных**: При первом запуске база данных создается автоматически. При изменении моделей выполняется автоматическая миграция.

5. **Бот должен быть администратором**: Убедитесь, что бот добавлен в группу как администратор с правами на:
   - Ограничение участников (restrict members)
   - Исключение участников (ban members)

## Лицензия

Этот проект создан для личного использования.

Всех кто будет воровать код без разрешения палкой буду бить!
